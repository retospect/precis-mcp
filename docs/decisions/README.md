# ADR index

Architecture Decision Records — one file per decision, numbered in
the order they were taken. Files are never deleted; obsolete
decisions are marked **superseded** and kept for history.

Per AGENTS.md: "sorted by number; never delete, only supersede".

Fully-superseded ADRs may be relocated to
[`archive/`](./archive/README.md) to keep this listing focused on
currently-authoritative decisions — see
[ADR 0059](./0059-decision-log-archive-convention.md) for the
"Rest in Git" archive convention. Nothing is archived yet.

## By topic — current authoritative ADR

| Topic | Current ADR | Notes |
|---|---|---|
| Repo merge (acatome → precis) | [0001](./0001-merge-acatome-into-precis.md) | foundational |
| Tabular output format (TOON) | [0002](./0002-pub-id-and-toon.md) | TOON portion in force; identifier portion superseded |
| Shared tool registry | [0003](./0003-shared-tool-registry.md) | |
| Dockerfile layout | [0004](./0004-multi-stage-dockerfile.md) → [0009](./0009-dockerfile-relocation-container-first.md) | 0009 relocated the file; 0004 still describes the stage layering |
| Migration discipline (forward-only) | [0005](./0005-greenfield-migrations.md) | governs every `*.sql` edit |
| Install path (baseline snapshot) | [0031](./0031-baseline-snapshot-dual-track.md) | fresh DBs load `migrations/baseline/schema.sql`; dual-track, not a greenfield (cf. [0019](./0019-second-greenfield.md)) |
| Identifier scheme | [0036](./0036-universal-handles.md) | **accepted + implemented** (`utils/handle_registry.py`); one universal handle per record + chunk; supersedes 0006/0008 as the current address form. Lineage: 0002 §id → 0006 → 0008 → 0036 |
| Derived queue pattern | [0017](./0017-derived-queue-family.md) | extends 0007 (chunk-level → family registry) |
| Database backend | [0010](./0010-postgres-pgvector-system-of-record.md) | |
| Secrets management | [0055](./0055-secrets-vault.md) | **proposed**; DB-resident encrypted secrets, key in server config so `pg_dump` is shareable, DB-enforced RBAC (list/mask/reveal), thin Python wrapper. Design: [`docs/design/secrets-vault.md`](../design/secrets-vault.md) |
| Diagram element→chunk binding + `mermaid` kind | [0057](./0057-diagram-chunk-binding-and-mermaid.md) | **proposed (2026-07-15)**; a diagram element (SVG shape / mermaid node) binds to a chunk (`dc…`/paper/memory) via the **hybrid** model — the source `id=` is the join key, a chunk→chunk `depicts` link carries `meta.element` as the edge (zero sanitizer change, reverse-queryable via `chunk_connections`, dangling-binding lint). `render_diagram_context` surfaces every element + coords/topology + linked chunk body (the `_render_anchor_context` pattern generalized) into both the turn prompt and `get()`; the turn JSON grows a `links` array. `figure` + `mermaid` factor into a shared `src/precis/diagram/` core behind a `DiagramLang` port; **mermaid validate/render/export is pure-Python via `mermaidx`** (embedded QuickJS + resvg — no Node/Chromium/container; `[mermaid]` extra, `PRECIS_MERMAID_ENABLED` dark). `depicts`/`mermaid-of` relations seeded via 0056 open vocab; autonomous `diagram_propose` tick on the 0044 lane. Design: [`docs/design/diagram-editing-and-chunk-binding.md`](../design/diagram-editing-and-chunk-binding.md) |
| Chemistry / protein tool-packs | [0056](./0056-chemistry-tool-packs-plugin-route-kind.md) | **proposed (2026-07-14)**; external chem/protein compute (retrosynthesis, AlphaFold, sequence design) folds into precis as **plugin tool-packs**, not broker MCP servers — each tool = a **kind** (legible IR) + a **`job_type`** on the 0044 derived-job lane. One canonical `route` kind, engines normalize to it via LinChemIn at ingest; `protein`/`sequence` are siblings. Two enabling core seams **shipped+deployed** (`c13281e1`): `KindSpec.can_own_jobs` + open relation vocabulary (`validate_relation(store=…)` reads the live `relations` table so plugins seed their own relations, no core edit to the closed literal). Engines run on Linux nodes (Macs orchestrate); GPU-native-in-process (AlphaFold/DFT) vs portable-CPU-container (build-on-demand podman, weights from NAS, no tarball store/registry). Shareable (plugin+Dockerfiles+compose) in precis-mcp, fleet-private (inventory+topology+secrets) in `~/work/cluster`. Known gap: gripe 160213 (read-time inverse rewrite). Design: [`docs/design/chem-tools-integration.md`](../design/chem-tools-integration.md) |
| Dev image (Claude Code + UID/GID) | [0011](./0011-claude-in-dev-image.md) | |
| Model weights in runtime image | [0012](./0012-bake-models-into-runtime-image.md) | cold-build mitigation in [0019](./0019-premodels-build-context.md) |
| MCP session context | [0013](./0013-mcp-session-context-env-vars.md) | env-var triple |
| PDF metadata write-back | [0014](./0014-pdf-metadata-writeback.md) | |
| Marker memory leak | [0015](./0015-marker-leak-mitigation.md) | |
| Work-claim locking | [0016](./0016-advisory-lock-claims.md) | postgres advisory locks; replaces file-based |
| Discovery layer | **superseded** — see [F20 note in CLAUDE.md](../../CLAUDE.md) | [0018](./0018-persistent-discovery-layer.md) kept for history; per-chunk KeyBERT now lives in `src/precis/workers/chunk_keywords.py` |
| Embedder as a service | [0020](./0020-embedder-as-service.md) | **accepted**; client+service+CLI landed (CUDA image + launchd pending) |
| Image split (serve/worker/ingest) | [0021](./0021-image-split-serve-worker-ingest.md) | **accepted**; serve/worker/ingest/embedder targets + build-all landed |
| Independent worker queues | [0022](./0022-independent-worker-queues.md) | **proposed**; extends 0016/0017 |
| `view='dreamable'` (ANN ring, no clustering dep) | [0023](./0023-dreamable-no-clustering-dep.md) | **accepted** |
| Dream loop runtime | [0024](./0024-dream-loop-litellm-inprocess.md) | **superseded / reversed** — in-process litellm abandoned; dream runs the `claude` binary (`utils/claude_agent.py`) |
| In-place cluster reconcile (not a third greenfield) | [0025](./0025-in-place-reconcile-not-third-greenfield.md) | **accepted**; does not supersede 0019 |
| precis-web as sibling package | [0026](./0026-precis-web-surface.md) | **accepted**; browser UI over the handler layer |
| Reparent todos via `parent` link relation | [0027](./0027-reparent-via-parent-link.md) | **accepted**; supersedes the `parent_id` column path for reparenting |
| Host heartbeat telemetry (Status tab) | [0028](./0028-host-heartbeat-telemetry.md) | **accepted**; extends 0026 |
| Multi-root corpus for PDF serving | [0029](./0029-multi-root-corpus-pdf.md) | **accepted**; `PRECIS_CORPUS_DIR` accepts a list of roots |
| `job` / `finding` / `cron` stay separate from `todo` | [0030](./0030-job-finding-cron-stay-separate.md) → [0061](./0061-fold-cron-into-recurring.md) (cron section only) | **accepted**; `job`/`finding` rulings still in force. The `cron` ruling is superseded by 0061 — `kind='cron'` retired, folded onto `level:recurring` + `meta.deliver` |
| Drafts as editable chunk-native documents | [0033](./0033-draft-chunks-editable-document.md) | **accepted**; the `draft` kind |
| Figure assets + data supplements + permission provenance | [0034](./0034-figure-assets-and-permission-provenance.md) | **draft/proposed**; figures as chunks, blobs in `chunk_blobs` |
| Computed chunks (payload + recipe), sandboxed execution, recompute boundary | [0035](./0035-computed-chunks-recipes-and-the-recompute-boundary.md) | **draft/proposed**; refines 0034 §3 — data/render recipes, `plots` the only reactive edge |
| Universal handles (one address system for every record + chunk) | [0036](./0036-universal-handles.md) | **draft/proposed**; flat type-prefixed Crockford handles; partially supersedes 0033 §1 (draft handle/sigil) |
| Heading styles (self-contained sections) + numbering lock + issues | [0037](./0037-heading-styles-and-numbering-lock.md) | **proposed**; per-heading style=skill, genre=root style, entity-bound numbering with pinned/lock, anchored `issue` loop; extends 0033/0034/0035, uses 0036 handles. Drafting catalogue: `docs/design/draft-section-styles.md` |
| Prompt assembly & prompt-engineering principles | [0038](./0038-prompt-assembly-and-principles.md) | **proposed**; one assembler + module library (markdown+frontmatter+`{{include}}`), cached/variable layers, agent/helper profiles, per-target adapters, the doc_context/tools/kinds/glossary tables, kind=code alias, conditional modules. Validation: `docs/design/prompt-assembly-shots.md` |
| ORCID author kind & network discovery | [0039](./0039-orcid-author-kind-and-network-discovery.md) | **proposed**; `kind='orcid'` durable author node (slug=iD, embedded card), `authored`/`authored-by` ref→ref links w/ position meta, missing-DOI diff auto-enqueues stubs into the existing fetch_oa pipeline, S2 author endpoint (`authors:`/`author:`) for paper→author→paper BFS; uses 0036 handles, feeds 0030 stub→fetch. Skills: `precis-orcid-help`, author-discovery |
| `cad` kind — analytic-IR solid design | [0041](./0041-cad-kind-analytic-ir.md) | **proposed (v2)**; own a curated analytic-primitive IR (probe/inspect analytically, **never mesh or merge to inspect**), OpenSCAD/OCCT as *export* backends only. mm/float64, **rigid-only transforms ⇒ everything exact**. Nodes = a DAG stored as a flat chunk-list (primitives + `merge`/`subtract`/`intersect`/`move`/`pattern`/`instance` operators); generalized **frustum** (box/cyl/cone/hex/pyramid/taper) + sphere + torus, **chamfers** for edges; the **membership contract** is the exclusion line (no hull/minkowski). Eyes = a full-DOF probe ladder (point/line/arc/section, interval arithmetic, TOON output — no pixels, no GL); feature-attributed sections; subtraction visible via op-fold; clearance/interference/translational-DOF as persisted **observers**; **datums** (named axes/planes). Handle code `ca`; mutable soft-deletable chunks (0033), `draft_export`-style worker, artifacts on 0029. Threads/gears/rotational-DOF/fillets/STEP-OCCT phase 2. Skill: `precis-cad-help` |
| The `pcb` kind: netlist + placement IR (JLCPCB-native) | [0042](./0042-pcb-kind-netlist-placement-ir.md) | **proposed**; electronics sibling of [0041] — own the netlist + part-selection + placement IR (LLM reads it as graph/ratsnest, not pixels), rent the autorouter (Freerouting/EasyEDA) + fab (JLCPCB) at export. `kind='pcb'`(`pb`) two-layer (logical nets / physical placement); **stored in dedicated relational tables, NOT chunks** (converges with 0041 Amendment 1 — dedicated tables + one card chunk; 0042 goes multi-table since a netlist wants SQL+FK integrity; design stays a ref with one card chunk); type/instance split (component-type owns pins; instances are placements) + netlist as `pcb_netconns`(net,instance,pin); soft "measure" rows (bypass/terminator/sensitive↔noisy intent on a hard↔soft↔gauge spectrum, the "measuring tapes," re-evaluated like 0041 observers); `note` on every row (the why-of-a-wire); `fixed` mark (screw holes/status LED); `parts` catalog table JLCPCB-assemblable+high-turnover selector; BOM/CPL as views; datasheets as a thin `datasheet`(`da`) PaperHandler sibling (capped: one kind for the whole electronics-doc family, machinery shared); crossing-minimizing auto-place + route-feasibility estimate; phase-2 LLM-piloted "shove router" (own routing, rent gerbers). Builds on 0041(converges on storage w/ its Amdt 1)/0033/0035/0036. Skills: `precis-pcb-help`, `precis-part-select-help`, `precis-net-class-help`, `precis-measures-help`, decoupling/i2c/spi/datasheet |
| The `structure` kind: atomistic cell + bond-graph IR | [0043](./0043-structure-kind-atomistic-ir.md) | **shipped**; materials sibling of 0041/0042 — own a legible cell+atoms+bond-graph IR the LLM reads as structure not pixels, rent the relaxer (MACE/GPAW) at the energy-rung ladder via the §23.16 run-cube cache; typed ops + in-memory probes; cursors/measures on `struct_measures`; `derived-from` lineage + `StructureHandler.derive`; web viewer + instruction box. Skill: `precis-structure-help` |
| The derived-job lane: a job parents on its subject, not a todo | [0044](./0044-derived-job-lane.md) | **accepted (2026-07-02)**; a `kind='job'` parent is polymorphic — a `todo` (intent lane: rotation + `child-failed` bubble + `child_job_succeeded`) or a build subject (`structure`/`cad`/`draft`, compute lane: idempotent, cache-fillable, owned by the artifact). The lane is **emergent from the parent kind**, not a declared flag. An intentful requester that wants to block links `requested`→job (migration 0046, inverse `requested-by`); `derived_job_succeeded` evaluator closes it on success, the failure-bubble follows the link to tag it on failure. Removes 0043's "relax needs a parent todo" requirement; extends 0007's derived-artifact philosophy to cross-host compute. |
| The `folder` kind: placement, kind roles, spanning search | [0045](./0045-folder-kind-placement-and-roles.md) | **accepted**; extrinsic single-parent containment on `refs.parent_id` (derivation ≠ placement); generalizes 0027's virtual `parent` façade beyond todo; `KindSpec.role` (artifact/corpus/stream/system) gates placeability + default search scope; todo roots may sit in folders (kind-aware root predicate); virtual Unfiled, refuse-non-empty delete, shallow-folder discipline; `folder=` subtree scope on cross-kind search. Skill: `precis-folder-help` |
| LLM routing layer — one seam for model + transport + result | [0046](./0046-llm-routing-layer.md) | **proposed**; unit 4a+4b landed (`src/precis/utils/llm/router.py`). Consolidates ~a-dozen scattered `os.environ.get` model reads into one `resolve_model(tier)` table (cloud-super/opus, cloud-mid/sonnet, cloud-small/haiku, local-small/summarizer, local-big/qwen-heavy — the `PRECIS_MODEL_*` triad pins); one `dispatch(LlmRequest)` seam over the three transports (`claude_agent`/`claude_p`/litellm `LlmClient`) with transport chosen by `(tier, tools_needed)` aligned to the 0038 `Profile` (AGENT↔tools↔claude_agent, HELPER↔claude_p/litellm); one normalized `LlmResult` unifying the JSON-block / stream-json / OpenAI-choices shapes. Rogue subprocess sites (plan_tick/fix_gripe/tex_llm_fix) route model via the resolver + plan_tick gains a budget cap (4b). `local-big + MCP tools` is the **documented next step** (re-lands the reversed [0024] in-process-litellm-with-`tools=` path behind the seam). Extends [0038] |
| Controlled chunk tagging: closed faceted vocabulary, one machine tagger, offline curation | [0047](./0047-controlled-chunk-tagging.md) | **accepted** (implemented 2026-07-04; shipped as the `junk`→`ROLE3` cascade — 88%/91%-own-precision on the free model — since the 11-way `role:` needs a stronger model. As-built: `docs/design/chunk-classifier-cascade.md`, `scripts/classify/EVAL_RESULTS.md`); facet tags as one more derived chunk artifact (0007): versioned axis YAML (extends `data/axes/` — whose 7 ref axes have NEVER been applied on prod — with `level`/`applies_to_kinds`/`select`/`aliases`/`context`), chunk axes only for what varies within a document: provenance-oriented `role:` first (result vs related-work vs boilerplate — protects `\citequote`), `method:`/`process:` mined+gated later. One writer: `chunk_tag` pass on the `chunk_claims` lease + `chunks.meta->'tagging'` envelope (version bump = lazy re-tag); teacher–student (LLM defines/labels silver, linear head over existing bge-m3 vectors sweeps 1.4M chunks, cascade escalation). Minting offline-only: miner proposes (unknown-rate + keywords + cluster tiles; clusters propose, prompts dispose), PR admits (≥30-ref spread, gold eval ≥85% + confusion/calibration), tag-verb prefix validation makes finiteness structural; curated tag_embeddings embed slug+definition. Iterative rounds coarse→dense: ref axes on 9.4k papers → chunk role → gazetteers → density-gated refinements. Motivated by prod folksonomy drift (52% singleton OPEN tags, `interest:`/`topic:` split). |
| Autonomous backlog execution — the laptop fixer loop | [0048](./0048-autonomous-backlog-execution.md) | **accepted (design, 2026-07-04); MVP pending build**. Wrap the proven `/go` core (`scripts/ship`+`scripts/deploy`) in an autonomous loop: two gates of different natures — **`ready`** (a `claude_p` judge; entrance; the weld = mandatory acceptance criteria; *is* the auto-safety classifier, run on gripes too) and the deterministic **ship** gate (exit). Specs = transient ADR-shaped **files** in `docs/proposals/` (discourse stays a conversation). **Two schedulers, substrate-matched**: repo-dev runs a **git-world CI loop (the fixer) NOT precis dispatch** (precis = source+sink); content stays on dispatch. Host = **the laptop** (dodges the self-deploy ouroboros — not a deploy target; launchd/skip-on-battery/Amphetamine); `fix_gripe` keeps its `run()` guts, retires the dispatch coupling, generalizes gripe→work-item. Keyless deploy: loop runs full `/go`; **fix-forward only** (no rollback/circuit-breaker/migration-detector — manual backup restore is break-glass); post-deploy is an **agentic look-at-prod → fix-forward**, not a scripted smoke. Reporting **by exception** (silent green / one-line fix / loud needs-you) to **#news** + `agentlog` + gripe comment + `/whatneedsdoing`. Retires the dispatch coupling of [0044]'s `fix_gripe`; keeps repo-dev out of precis dispatch per [0030]. |
| The `fluidics` kind: microfluidic network + constraint IR | [0050](./0050-fluidics-kind-network-ir.md) | **proposed / discussion (open — not closed)**; microfluidics sibling of 0041/0042/0043 — own a legible netlist + declarative-constraint IR the LLM reasons over as a graph, rent routing/sizing/fab-file synthesis at export as 0044 derived jobs. Three-layer IR (netlist / constraints / placement-geometry); a **layer-stack** model where adhesive is just a layer with thickness and **depth is a pierced-layer span not a scalar** (subsumes adhesive-height + laminate quantization + deep channels); three **bond-types** — `adhesive` (cut void → void-boundary DXF), `weld` (film–film, seam → weld-path DXF, compliant depth), `mill` (molded core → G-code) — a design may mix them (film–adhesive–moldedpart–adhesive–film emits both). Valves carry a `shape×stroke` **displacement volume** (dead volume ⇄ pump stroke volume); **composite macros + actuation programs** (the pseudo-peristaltic 3-valve pump); the **compartment mechanic** (delete closed-valve edges → connected components = metered compartments; pure graph, no CFD). Constraints **split by cost** (cheap graph checks / bounded LP-MILP sizer / DRC / deferred autoroute); DRC bidirectional (min-pitch isolation + max-pitch pump proximity). **Bubbles are a *predicted* hazard** (geometry+priming analysis + purge-path check), not a graph element. v1 excludes rotary valves, compliant-volume, autorouting. Handle `fl`. Slice 0 = netlist IR + probes + lint. Six open forks. |
| Figure medium axis + the source resolver (blob / canvas / graph) | [0058](./0058-figure-medium-axis-and-source-resolver.md) | **proposed (2026-07-15)**; extends 0034/0035. Splits the figure model into **two orthogonal axes** — `origin` (rights: original/own_graph/third_party, drives clearance only) × `medium` (production: `blob`/`canvas`/`graph`, drives render+edit+export) — behind one `FigureSource` resolver the reader/export/clearance call. **Medium is a contract, not a format**: Mermaid/TikZ/dot are *languages* under the `canvas` contract via the figure kind's existing `meta.render` slot, so a new format touches only the compiler, never the medium enum or any consumer. Pointer discipline — only `blob` owns bytes; `canvas` points **by link** (`has-figure`, chunk→ref), `graph` by `plots`+recipe (0035). Clearance becomes `origin × medium` and stops green-lighting asset-less figures. **No migration** (links already carry chunk endpoints; `has-figure`/`figure-of` already relations; `meta.render` already a language slot). Motivating bug: draft *Deck Hook* — 5 caption-only figures rendering as broken images while the reader reports "✓ all 5 cleared to ship". Slices: `canvas` end-to-end (this branch) → Mermaid-under-canvas (own branch) → blob-SVG sniff+sanitize → export. |
| The argument graph — lemmas, inferences, the reasoning shadow beside a draft | [0054](./0054-argument-graph-lemmas-inferences-reasoning-shadow.md) | **proposed (2026-07-10)**; the logical skeleton of a draft's arguments, persisted as a queryable graph and kept out of the published prose. Applies the 0030 kind test and concludes **no new kind** — lemmas/inferences are `memory` sub-kinds (`kind:lemma`/`kind:inference`), empirical lemmas stay `finding`. Adds **two** relations `entails`/`entailed-by` (inference node → conclusion; premises attach via reused `derived-from`, the node reifying the conjunction) + `qualifies`/`qualified-by` (caveat/rebuttal node → the claim it bounds); one closed tag axis `TRUST:` (asserted judgment, not computed). **Relations stay closed** — a relation is a behavioral contract (code branches on it) not a label; author-minted labels rejected (re-imports 0047 folksonomy drift + breaks auto-mirror), open nuance rides in edge `meta`. Operator is a **label** (`meta.rule`+`meta.warrant`), **not** a checked proof — rejects a validity engine, a computed trust scalar, and a defeasible-logic caveat-discharge engine as tar-pits. The features that earn it: **retraction-ripple** (a `retracts` edge on a cited source flags downstream inferences stale) + **caveat-inheritance** (a source's conditional claim never silently loses its rebuttal), both **display-only** propagation via `view='argument'` (v1) / a walker (phase 2). Division of labor (§8): the LLM reads/judges, the code remembers/routes — so the value-bearing propagation **must be code, not a skill**; the discipline + `kind:lemma`/`:inference`/`:caveat` sub-kinds are skill/open-tag only. No migration. Never exports to `\cite` (0033/finding boundary). Feeds 0051's blackboard convergence; follows 0052's shadow-registry precedent. Skill sketches + build order: `docs/design/argument-graph.md` |
| Decision-log archive convention ("Rest in Git") | [0059](./0059-decision-log-archive-convention.md) | **accepted (2026-07-15)**; docs-only. Establishes [`archive/`](./archive/README.md) for fully-superseded ADRs: move-not-delete (filename kept, git history follows), one-line archive banner as the only edit to the sealed body, and every referrer (this index, the supersession graph, relative links) updated in the same change. An ADR is archivable only when a live successor already names it. Nothing archived yet; each chain condensation is its own reviewed change. |
| Topic dossiers: standing paper→topic classification, quest-family syntheses, digest cast | [0060](./0060-topic-dossiers.md) | **proposed (2026-07-22)**; extends 0047's cascade one level up (paper title+abstract → **multi-label** `topic:` tags, cheap tier-0/1 + escalate tier-2), keeping the *top-level* topic list closed (folksonomy-drift lesson from 0047) while sub-tags stay open. Reuses `quest`'s coordinator/dossier/WORM-log loop as-is with a new synthesis tick body (harvest unintegrated papers → merge into the dossier `draft` → log → link `integrated-into`, no new field). Seed topics: `healthspan` (one draft, many sub-tags: sleep/fitness/biomarkers/inflammation/data-driven), `molelec`, `noxrr` (existing catalyst-discovery quest, fed passively alongside its own active search), `llm-improvements`. Weekly synthesis tick drives two outputs: a shareable weekly digest cast (reuses `briefing_cast.py`'s lane-union pattern) + a quiet daily-brief lane. No migration. Design: `docs/design/topic-dossiers.md`. |
| Fold `kind='cron'` into `level:recurring` (reverses 0030's cron ruling) | [0061](./0061-fold-cron-into-recurring.md) | **accepted (2026-07-22)**; retires `kind='cron'` — one-shot scheduling moves onto a third `meta.schedule` shape (`at`, an absolute ISO 8601 fire time, self-retiring via `STATUS:done`), push delivery moves onto `meta.deliver = {'target': ...}` on a `level:recurring` todo. A due delivery-mode tick fires the **identical** `pg_notify('precis.cron', {cron_id, payload, target})` wire payload the retired kind fired, so asa_bot's listener needs no change — it never read a `kind='cron'` ref back, only the notify. No schema migration (kind is a plain string column); existing `cron` rows move via a one-off `--commit`-gated backfill script, **not yet run against prod** (tracked in `OPEN-ITEMS.md`). `job`/`finding` rulings of 0030 unaffected. |

## Supersession graph

```
0002 (identifier §)  ──→  0006  ──→  0008  ──→  0036   # slug → universal handles
0002 (TOON §)         (in force)
0033 §1 (draft handle/¶ sigil)  ──→  0036          # absorbed into universal handles
0007  ──→  0017                                    # derived queue
0007  ──→  0044            # derived-artifact philosophy → cross-host compute (job parents on its subject)
0043  ──→  0044            # structure relax no longer requires a parent todo (derived-job lane)
0030  ──→  0054            # kind test reused → lemmas/inferences are memory sub-kinds, NOT a new kind
0030  ──→  0061            # cron ruling reversed — kind='cron' retired, folded onto level:recurring + meta.deliver (job/finding rulings unaffected)
0033  ──→  0054            # draft the argument graph shadows; 0054 extends, never publishes to \cite
0051  ┄┄→  0054            # argument graph is a convergence substrate for the blackboard (feeds, not supersedes)
0052  ┄┄→  0054            # shadow-registry-beside-the-draft precedent
0043  ──→  0053            # external DFT catalyst libraries import as structure designs + run-cube rows
0044  ──→  0053            # a derivative of an imported config re-uses the derived-job compute lane
0044  ──→  0056            # chem/protein engines run on the derived-job compute lane (route/protein/sequence jobs)
0043  ┄┄→  0056            # keystone-IR discipline generalized: a route is the retrosynthesis IR (cf. 0041/0042)
0041  ┄┄→  0056            # own-a-legible-IR-rent-the-kernel precedent → canonical route kind, engines swappable

0007  ──┐
        ├──→  0018  ──→  F20 (CLAUDE.md, not an ADR)
0017  ──┘                # discovery layer — superseded post-ADR
0004  ──→  0009          # Dockerfile move (extends, not replaces)
0012  ──→  0019          # premodels build context (extends)
0024 (reversed)          # dream loop: in-process litellm → back to claude binary
0024  ┄┄→  0046          # local-big+tools: reversed litellm-with-tools re-scoped as the routing seam's next step
0038  ──→  0046          # prompt-assembly Profile → routing seam (model/transport/result); AGENT/HELPER ↔ Tier/Transport
0026  ──→  0028          # precis-web surface → host-heartbeat Status tab (extends)
0033  ──→  0034  ──→  0035  ──→  0058  # editable-document model: figures → computed chunks → figure medium axis + source resolver (each extends)
0034  ──→  0058            # origin split into two axes (rights × medium); blob/canvas/graph behind one resolver
0035  ──→  0058            # graph recipe becomes the `graph` medium
0033  ──→  0037            # heading styles / numbering / issues (extends; 0037 reframes design/patent-drafting-merge.md)
0033 §8 (editor prompt) ──→ 0038   # prompt assembly: one assembler + modules (uses 0036/0037)
0041  ──→  0042            # CAD: solids → electronics/PCB sibling (extends keystone; CONVERGES on storage w/ 0041 Amendment 1 — dedicated tables + 1 card chunk; 0042 goes multi-table relational)
0047  ┄┄→  0060            # cascade pattern lifted one level: chunk-tag classifier → paper-topic classifier (multi-label, closed top-level list)
```

## Conventions

- New ADRs get the next number; never reuse a number.
- An ADR can be **superseded** (replaced wholesale), **partially
  superseded** (one section replaced; others in force), or
  **extended** (later ADR builds on it without invalidating).
- The header of the *older* ADR should name its successor; the
  successor's header should name what it supersedes.
- When a feature ships outside the ADR process (e.g. F20), update
  the affected ADR's status line to point at the live code path
  and leave the ADR body intact.
- A fully-superseded ADR may be relocated to [`archive/`](./archive/README.md)
  under [ADR 0059](./0059-decision-log-archive-convention.md): keep its
  filename, prepend a one-line archive banner (the only edit to the sealed
  body), and update every referrer (this index, the supersession graph, and
  any relative link) in the same change.
