# Runbooks

One file per reusable operational procedure — forensics worth keeping from
an incident land here, never a done-log (`docs/README.md`).

**Index:** [`INDEX.md`](./INDEX.md) — one line per runbook.
Generated locally and gitignored; if the link target is missing or stale,
run `python3 scripts/docs-index` (stdlib-only, regenerated automatically at
session start).
