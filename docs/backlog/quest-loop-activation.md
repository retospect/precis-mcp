# Quest layer: link real quests + activate the loop

All slices shipped; the loop is dormant (quests paused 2026-07-16). Do:
derive strivings from docs/mission.md + live research programs
(`put(kind='quest')` + `link(rel='serves')`), re-activate, flip
PRECIS_QUEST_LOOP_ENABLED on the melchior agent worker. Prereq: the spark
struct_relax/gpaw-relax container lane must genuinely run (the infra-failure
classifier fix only stops misclassification). Ops follow-up: un-rule-out
st164913 (drop `ruled-out:relax-failed` + correct the dossier's "Pd(111)
unstable" text). Standing design questions: cost/credit attribution under
overlapping quests; a concrete "promise" bid proxy (frontier-improvement
rate); prose rubric → measurable objective vector; the propose-next-candidate
proposer is the crux and least specified.
