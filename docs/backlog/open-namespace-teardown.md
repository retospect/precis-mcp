# open-namespace-teardown

## Residuals (from OPEN-ITEMS)

Design awaiting Reto's review (open questions: `level:` axis-vs-column, the
`internal-thought` dual-writer, `sticky:`'s fate). This blocks the
OA-acquisition roadmap's §G referenced_works→topics wiring. Cull-rule
caution: match `namespace='OPEN' AND value LIKE 'p:%'`, never
`namespace LIKE 'OPEN%'` — that eats the ADR-0047 OPEN-QUESTION axis.
