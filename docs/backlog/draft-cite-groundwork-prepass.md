# Draft citation-groundwork pre-pass (ADR 0051 Level 2) — hold

A cheap-model tick that reads a section's cluster-map working set + its
Enhancement-marked gaps, drills the cited papers for the specific supporting
chunk, and rewrites [pa] → [pc] before the real edit turn runs. Proposed
home: a new thread_type in `src/precis/workers/thread_persona.py`, run ahead
of the edit tick. Decided: HOLD until the shipped write-time warning +
Hygiene footer prove insufficient — build only if agents keep leaving
whole-paper citations despite the nudge.
