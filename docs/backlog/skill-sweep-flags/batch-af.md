Slice-2 sweep flags, batch af (21 files: precis-plaintext-help through
precis-review-section-structure). One line per flag: `<file> — <section
heading> — <drift|pin> — <note>`.

precis-review-paragraph-flow.md — Output: one finding per paragraph — drift — `put(kind='finding', text=..., rel='paragraph-flow-finding')` omits `title=`/`cited_in=`; `finding.put`'s ordinary-mode requires title+body(text alias)+cited_in (`handlers/finding.py::FindingHandler.put`) — call as written raises `BadInput` listing missing title=/cited_in=.
precis-review-section-structure.md — Output: one finding per structural break — drift — same bug: `put(kind='finding', text=..., rel='section-structure-finding')` missing required `title=`/`cited_in=`; will not run as documented.
precis-polish-paper.md — Personas this runbook orchestrates — drift (fixed in this pass) — body used `[[skill:precis-adversarial-reviewer]]`/`[[skill:precis-citation-reviewer]]`; `WIKILINK_RE` (`_skill_common.py`) only matches `[a-z0-9][a-z0-9-]*`, no colon, so these were silently dropped by the graph parser (never extracted, never flagged dangling either). Corrected to `[[precis-adversarial-reviewer]]`/`[[precis-citation-reviewer]]` as part of this touch.
precis-preflight.md — frontmatter — drift (spec gap, not file-local) — `kinds:` left unset; `provenance` has no entry in `handle_registry.KIND_CODES` (stateless tool, addressed by query not handle) so `unknown_kinds()`/`is_known_kind('provenance')` returns False — there is no valid value this skill's actual subject kind could take in `kinds:`.
precis-provenance-help.md — frontmatter — drift (same spec gap) — same as above; `kinds: [provenance]` would fail the ingest gate.
precis-random-help.md — frontmatter — drift (same spec gap) — `random` also has no handle code (`is_known_kind('random')` is False); `kinds:` omitted for the same reason.
precis-replication.md — frontmatter — drift (same spec gap) — `applies-to` lists `kind='web'`, but `web` has no handle code either; `kinds:` carries only the valid subset (`paper, plan, todo, finding, memory`), so the `web` leg of this skill's actual scope is invisible to kind-filtered toc/footer surfacing. Filed as a gripe (provider/stateless kinds — provenance/random/web/calc/math/websearch/wikipedia/youtube/semanticscholar/perplexity-* — can never populate `kinds:` under the current handle-registry-backed validation).
precis-put-help.md — put(kind='websearch', mode='import', ...) — pin — mode='import' cache-seeding path is easy to silently break; worth a round-trip test.
precis-provenance-help.md — Check whether a DOI has been retracted — pin — `get(kind='provenance', id='10.1038/nature05095')` is a small, deterministic single-DOI lookup; good smoke-test candidate.
precis-random-help.md — Mint a random short identifier — pin — `get(kind='random', view='slug', args={...})` is cheap and deterministic modulo output value; worth pinning the length/alphabet contract.
precis-relations.md — Which relation should I use? — pin — the closed `rel=` vocabulary table is load-bearing; a round-trip test iterating the table and asserting every listed relation is accepted (and mirrors where documented) would catch vocabulary drift directly.
precis-python-help.md — Map a stack trace to a symbol — pin — `get(kind='python', id='...~L444')` → resolve-line-to-symbol is a core navigation path worth a fixed round-trip.
precis-quest-help.md — The logbook — a WORM ledger of the journey — pin — the `entry=` typed logbook append (`put(kind='quest', ..., entry='hypothesis')`) plus `view='tree'` render is high-traffic and worth pinning end to end.
precis-recurring-help.md — Schedule format — pin — the `every: '60m'` invalid-clamp example documents a validation boundary (m capped at 59); worth a round-trip test asserting the rejection, not just the accepted forms.
