## batch-ag flags

- `precis-tags.md` — ## What are the closed UPPERCASE axes? — drift — "To see the live set, `get(kind='skill', id='precis-status-help')`" points at the build/runtime introspection skill; that skill documents server build/DB/runtime facts, not STATUS tag values (the full 20-value union is already listed in the table two paragraphs above). Looks like a copy-paste mismatch from `precis-status`/`precis-status-help` — resolve the intended target or drop the pointer.
- `precis-self-consolidation-help.md` — ## Related skills — pin — bare bullet list under `## Related skills`, not `## See also`; same graph-edge intent, not converted per sweep scope (only `## See also` was in scope); candidate for heading normalization + wikilink conversion in a later pass.
- `precis-rxn-help.md` — ## Record a reaction, then add what sources report — pin — the `put(kind='rxn', ...)` create-then-append-sourced-value pair is a concrete two-call round-trip (mint + append fact, re-running the second call from "another paper"); good pin candidate.
- `precis-se-help.md` — ## Units and geometry conventions — read first — pin — the verified box/cyl AABB claim (`box:w0.028d0.240h0.020` → AABB x ±0.014, y ±0.12, z 0..0.02) is a specific, checkable geometry assertion against the cad kernel; good regression pin given the file notes an earlier revision got this wrong.
- `precis-se-design-help.md` — ## Worked example (in prod) — pin — `unicycle-printed-v1` (29 blocks, 12 tension-only axial spokes at 600 N, `first-order mobile` stability verdict) is a concrete live design; `view='tree'` then `view='stability'` is a good round-trip pin.
- `precis-search-help.md` — ### good=True — deep search (async campaign) — pin — the `search(..., good=True)` → poll `get(kind='job', id=...)` flow is a concrete async round-trip worth pinning.
- `precis-search-help.md` — ## Find sources a draft hasn't cited yet — pin — `uncited=<draft>` with the `_(uncited=dr173020: N already-cited sources excluded)_` response line is a deterministic, checkable round-trip.
- `precis-structure-help.md` — ## Place an adsorbate on a real site, not a guessed z — pin — the Pd(111) hollow-site numeric example (on-surface `z≈0.46–0.47` vs the `frac z=0.66` floating-atom failure case) is a specific, checkable claim; good regression pin.
- `precis-structure-help.md` — ## Relax — the fidelity ladder — pin — the `dispersion=True` D3-correction example plus the `model='chgnet'` rejection is a concrete behavioral claim worth pinning.
- `precis-taproot-mint-help.md` — ## Mint a claim hub from a claim I've already sourced — pin — the `put(kind='finding', supporters=[...])` worked example is the canonical hub-mint round-trip, high-traffic and concrete (verified `hub_ref_id` key against `handlers/finding.py`).
- `precis-taproot-backfill-help.md` — ## Turn a draft's [pc<id>] cites into a hub cite — pin — the `put(kind='todo', meta={'job_type': 'taproot_backfill', ...})` dispatch example is a concrete, high-traffic worked recipe.
- `precis-todo-help.md` — ## Inspect a todo's full record (debug meta) — pin — the `meta` key table (executor/schedule/auto_check/workspace/anchor) is a concrete, checkable reference worth a round-trip test confirming each key still drives the behavior claimed.
