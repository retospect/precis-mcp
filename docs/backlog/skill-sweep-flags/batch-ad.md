## batch-ad flags

- `precis-memory-help.md` — ## Sticky memories — drift — `tag(kind='memory', ..., ttl_days=...)` has no effect: `MemoryHandler.tag` (`handlers/memory.py::MemoryHandler.tag`) forwards `**_kw` straight to `NumericRefHandler.tag` (`handlers/_numeric_ref.py::NumericRefHandler.tag`), which never reads `ttl_days`/`expires_at` and calls `store.add_tag` with no `expires_at=` — `apply_tag_ops` (the function that actually resolves `ttl_days` into an expiry, `handlers/_link_tag_ops.py::apply_tag_ops`) is called by `citation`/`finding`/`paper`/`oracle`/`orcid`/`conversation`/`presentation`/`wikipedia`/cache-backed handlers, but not by `memory` or its `_numeric_ref` base. The doc's "30 days (the default TTL)" / "90 days (default)" claims and the `ttl_days=7`/`ttl_days=180` examples appear to document a feature that isn't wired for this kind — worth a design call: wire memory's sticky tags through `apply_tag_ops`, or correct the doc to say TTL/expiry isn't currently enforced on `sticky:*` for memory.
- `precis-minter-help.md` — ## Host capabilities (what an executor advertises) — drift — doc says `coordinator` has "empty PROVIDES"; `workers/executors/__init__.py::EXECUTOR_PROVIDES['coordinator']` is `frozenset({'claude_bin'})`, not empty (gr335087 exception for `quest_tick`'s inline LLM step, per the code comment there) — small factual correction needed, not made per scope (design-characterization call, not "obvious").
- `precis-minter-help.md` — ## Toolpath — write an intent, walk away — pin — the 3-step `put(kind='todo', meta.executor=...)` → `link(rel='fixes')` → `get(view='tree')` sequence is a concrete, verifiable round-trip (minter tick ≤ 1 min); good round-trip candidate against the dev DB.
- `precis-message-help.md` — ## Post with attachments — pin — the `put(kind='message', ..., attachments=[...])` shape matches `handlers/message.py` exactly (filename/content_type/archive_path); worth pinning once an NFS-backed dev fixture exists for `archive_path`.
