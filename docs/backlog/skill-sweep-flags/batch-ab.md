## batch-ab flags

- `precis-cache.md` — ## What TTL does a kind have? — drift — table claims `perplexity-reasoning` TTL is 30 days, but `handlers/perplexity.py::ThinkHandler.TIER` sets `ttl_seconds=None` (pinned, never expires) — `websearch` (7d) and `perplexity-research` (pinned) rows check out against `WebsearchHandler`/`ResearchHandler`; only the `perplexity-reasoning` row is stale.
- `precis-cache.md` — ## How is a cache key composed? — pin — the three-call cache-key-by-kind example (`websearch`/`perplexity-reasoning`/`perplexity-research` on the same `q=`) is concrete and cheap to round-trip once the TTL drift above is fixed.
- `precis-cad-help.md` — ## Author a design — put(id=<slug>, text=<source>) — pin — the `flange` node-list `put` (component/plate/cut/polar pattern) is the canonical minimal worked example for the whole kind; high-traffic, good round-trip anchor.
- `precis-cad-help.md` — ### Assemble by interface — port and mate — pin — the `nema17`/`drivetrain` port+mate example exercises pose derivation (`mate ... flip`) end to end; worth a dev-DB round-trip.
- `precis-calc-help.md` — ## Unit conversion (local, via pint) — pin — the `3 ft to m` / `100 degC to degF` table of worked conversions is deterministic and cheap; good candidate for a literal-output pin.
- `precis-cite-paper-help.md` — ## inline handle vs finding vs citation vs bibtex — which do I use? — pin — the decision table is referenced by several sibling skills as the canonical router; worth pinning the table content itself against a drift test, not just a call.
- `precis-cloze.md` — ## Gotcha — adjacent `}` truncates the cloze — pin — the `\sqrt{x^{2}}` → `\sqrt{x^{2` truncation claim is a concrete, surprising, and testable behavior; good round-trip candidate to confirm against the current cloze storage path.
- `precis-component-help.md` — ## Don't hand-enter a standard part — mint it from a series — pin — the `put(kind='component', series='iso-4762', size='M6x30')` mint call with its full expected spec dump is a strong round-trip candidate (idempotency + slug derivation both testable).
- `precis-datasheet-help.md` — ## Where a datasheet comes from — drift — doc gives the cluster inbox path as `/opt/nas/botshome/papers/inbox/datasheets/`; not re-verified against current `PRECIS_WATCH_INBOX` deploy config in this sweep (flagging for a deploy-config cross-check, not confirmed wrong).
- `precis-edgar-help.md` — ## Quickstart — get an NVIDIA filing — pin — the `ticker:nvda` / `cik:1045810` resolve-then-fetch-then-diff sequence is a clean, cheap, real-API round-trip (no auth needed) — good slice-3 candidate.
- `precis-edit-help.md` — ## When find= matches more than once / isn't found — pin — the two error-message worked examples (ambiguous match listing, fuzzy-nearest-line suggestion) are concrete and testable against the actual error formatter.
- `precis-estimate-help.md` — ## Structure tier — pin — the `view='compare'` + `args={'ops': [...]}` what-if example is the kind's most complex worked call and matches the handler code exactly (verified against `precis_estimate/handler.py::EstimateHandler`); strong round-trip candidate.
