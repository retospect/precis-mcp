{% extends "base.html.j2" %}
{% block title %}{{ ref.title }}{% endblock %}
{% block content %}
{# Slide-deck editor. Reuses the shared two-pane reader (Navigate/Jump/PDF
   + jump-to-page) from _reader/reader.html.j2 — pres joined _DOC_FAMILY so
   its slides get the same in-doc search / TOC / chunk-gloss sidebar. The
   Meta tab (doc.meta_panel = pres/_meta_panel.html.j2) is pres-specific:
   the attribution form + BibTeX + a clickable slide list. #}
{% include "_reader/reader.html.j2" %}
{% endblock %}
