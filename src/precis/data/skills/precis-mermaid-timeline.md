---
id: precis-mermaid-timeline
title: precis — timeline / chronology / history / roadmap of events
summary: a mermaid timeline — dated events in chronological order
applies-to: kind='mermaid'
status: active
---
A **timeline** lays out dated events in chronological order (a history, a
roadmap of milestones). Reach for it when the user says "timeline",
"chronology", "history", or "roadmap". For a schedule with **durations** (a
gantt) see `precis-mermaid-unsupported`.

    timeline
      title Company history
      2002 : Founded
      2010 : Series A
      2020 : IPO

CRUD, bindings, the /mermaid canvas → `precis-mermaid-help`. Authoring craft →
`precis-mermaid`.
