"""Bundled oracle wisdom YAMLs.

Loaded by :mod:`precis.jobs.ingest_oracles` — one YAML per
tradition. The files ship inside the wheel so ``precis jobs
ingest-oracles`` works out of the box without a separate data
download.

To regenerate or extend a tradition, edit the YAML in place and
re-run ``precis jobs ingest-oracles --overwrite``.
"""
