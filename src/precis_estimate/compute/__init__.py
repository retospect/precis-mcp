"""Compute tiers for the `estimate` kind. `composition.py` is slice 1
(element-property lookup, no geometry). Slice 2 adds a structure tier here
(coordination/strain/d-band on a held `structure` ref) and an ops what-if
tier — see `docs/backlog/estimate-kind-ms-chemistry-workup.md`.
"""

from __future__ import annotations
