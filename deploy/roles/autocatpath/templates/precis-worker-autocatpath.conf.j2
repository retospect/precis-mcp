# {{ ansible_managed }}
# Sources the autocatpath env (PRECIS_NODE / PRECIS_AUTOCATPATH_ROUTE_NODE) into
# precis-worker. `-` makes the file optional so the unit still starts when
# autocatpath is absent.
[Service]
EnvironmentFile=-/etc/precis/autocatpath.env
