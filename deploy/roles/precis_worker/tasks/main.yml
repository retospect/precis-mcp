---
# §L-b split (mirrors precis_worker_agent): provision.yml (venv/deps/seed)
# then units.yml (split-unit render). Playbook 20 imports this whole role
# (rollback path); 20b imports tasks_from: provision only.
- ansible.builtin.import_tasks: provision.yml
- ansible.builtin.import_tasks: units.yml
